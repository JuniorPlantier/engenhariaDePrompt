crie um script em java que recebe um arquivo CSV onde a primeira coluna é um idpedido e a segunda coluna é variavel, esse robo vai gerar o seguinte script com os dados do csv: update nome_tabela set valor_variavel = variavel where pedido = idpedido. Desconsidere a primeira linha que é o cabeçalho do csv, quebre em no maximo 100 updates por arquivo, cada arquivo deve começar com start transaction e finalizar com commit, o arquivo de entrada deve ser um parametro e o locar dos arquivos de saida é a mesma pasta do de entrada com dentro da pasta saída, criada pelo script java.
